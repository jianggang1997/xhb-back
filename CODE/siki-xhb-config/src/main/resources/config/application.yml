server:
  port: 30000

management:
  server:
    port: 39000
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS

eureka:
  instance:
    instance-id: ${spring.cloud.client.ipaddress}:${server.port}
    health-check-url: http://${spring.cloud.client.ipaddress}:${management.server.port}/actuator/health
    prefer-ip-address: true
  client:
    service-url:
      defaultZone:  http://192.168.37.129:30001/eureka/,http://192.168.37.131:30001/eureka/

spring:
  application:
    name: '@artifactId@'
  banner:
    location: classpath:config/banner.txt
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jianggang1997/xhb-back.git
          search-paths: siki-xhb-config-repository/{application}
          username: jianggang1997
          password: abc13797347245

info:
  name: iov-sjjh-servicenode-user
  version: '@project.version@'